using System;

namespace URI_1117
{
    class Program
    {
        static void Main(string[] args)
        {
            double nAv1 = int.Parse(Console.ReadLine());


            double media = 0.0;
            while (nAv1 < 0.0 & nAv1 > 10.0)
            {
                Console.WriteLine("nota invalida");
                nAv1 = int.Parse(Console.ReadLine());

            }
            // calculo
            double nAv2 = int.Parse(Console.ReadLine());
            while (nAv2 < 0.0 & nAv2 > 10.0)
            {
                Console.WriteLine("nota invalida");
                nAv2 = int.Parse(Console.ReadLine());

            }
            // calculo






        }
    }
}
